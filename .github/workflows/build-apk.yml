name: Build Mishwark APKs

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Test GitHub Actions
        run: echo "Mishwark workflow is working"